# GitHub Publication Audit — 2026-08-17

## Status

**VERIFIED — scoped public repository audit**

Current repository audit:

- Tracked files: `86`.
- Model-weight files (`.pt`, `.safetensors`, `.gguf`, `.bin`, `.ckpt`): `0`.
- Secret-like tracked files: `0`.
- Dataset payloads (`.jsonl`): `0`.
- Dataset-related files are manifests, reconciliation evidence, and a read-only audit script only.
- Working tree: clean.

The public repository contains code, contracts, manifests, reports, and smoke results. It does not publish model weights, the real MK7 Dataset, Gemma, Golden Training, or secrets.
